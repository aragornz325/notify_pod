class: DeviceToken
table: serverpod_device_token
fields:
    token: String
    userId: String
    type: DevicesType
    deviceId: String
    createdAt: DateTime
    updatedAt: DateTime
    register: List<Register>?, relation(name=deviceToken_register)